local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")

local BACKEND_URL = "https://mi-key-system.onrender.com"
local SCRIPT_URL = "https://raw.githubusercontent.com/luis32405516-cmyk/-1-VENTILADOR-POR-FAMA/refs/heads/main/SCRIPT"
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KeySystemGui"
ScreenGui.Parent = CoreGui or LocalPlayer:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 320, 0, 220)
Frame.Position = UDim2.new(0.5, -160, 0.5, -110)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Title.Text = "Sistema de Key"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18
Title.Font = Enum.Font.SourceSansBold
Title.Parent = Frame

local TextBox = Instance.new("TextBox")
TextBox.Size = UDim2.new(0.85, 0, 0, 35)
TextBox.Position = UDim2.new(0.075, 0, 0.25, 0)
TextBox.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
TextBox.PlaceholderText = "Pega tu key aquí..."
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 14
TextBox.Parent = Frame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.85, 0, 0, 25)
StatusLabel.Position = UDim2.new(0.075, 0, 0.45, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Esperando entrada..."
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.TextSize = 13
StatusLabel.Parent = Frame

local function ValidarEnServidor(proveedor, key)
    local endpoint = BACKEND_URL .. "/api/verify-" .. proveedor
    local payload = HttpService:JSONEncode({
        key = key,
        user_id = tostring(LocalPlayer.UserId)
    })
    
    local success, response = pcall(function()
        return request({
            Url = endpoint,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = payload
        })
    end)
    
    if success and response.StatusCode == 200 then
        local data = HttpService:JSONDecode(response.Body)
        return data.valid == true
    end
    return false
end

local function CargarScriptPrincipal()
    local success, err = pcall(function()
        loadstring(game:HttpGet(SCRIPT_URL))()
    end)
    if not success then
        warn("Error al cargar el script principal:", err)
    end
end

local BtnLootlabs = Instance.new("TextButton")
BtnLootlabs.Size = UDim2.new(0.4, 0, 0, 35)
BtnLootlabs.Position = UDim2.new(0.075, 0, 0.65, 0)
BtnLootlabs.BackgroundColor3 = Color3.fromRGB(0, 120, 215)
BtnLootlabs.Text = "Verificar LootLabs"
BtnLootlabs.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnLootlabs.TextSize = 12
BtnLootlabs.Parent = Frame

BtnLootlabs.MouseButton1Click:Connect(function()
    local key = TextBox.Text
    if key == "" then
        StatusLabel.Text = "Por favor ingresa una key"
        return
    end
    
    StatusLabel.Text = "Verificando en LootLabs..."
    if ValidarEnServidor("lootlabs", key) then
        StatusLabel.Text = "¡Key Válida! Cargando script..."
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        task.wait(1.5)
        ScreenGui:Destroy()
        
        CargarScriptPrincipal()
    else
        StatusLabel.Text = "Key inválida o expirada"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    end
end)

local BtnPlatoboost = Instance.new("TextButton")
BtnPlatoboost.Size = UDim2.new(0.4, 0, 0, 35)
BtnPlatoboost.Position = UDim2.new(0.525, 0, 0.65, 0)
BtnPlatoboost.BackgroundColor3 = Color3.fromRGB(120, 0, 215)
BtnPlatoboost.Text = "Verificar Platoboost"
BtnPlatoboost.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnPlatoboost.TextSize = 12
BtnPlatoboost.Parent = Frame

BtnPlatoboost.MouseButton1Click:Connect(function()
    local key = TextBox.Text
    if key == "" then
        StatusLabel.Text = "Por favor ingresa una key"
        return
    end
    
    StatusLabel.Text = "Verificando en Platoboost..."
    if ValidarEnServidor("platoboost", key) then
        StatusLabel.Text = "¡Key Válida! Cargando script..."
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        task.wait(1.5)
        ScreenGui:Destroy()
        
        CargarScriptPrincipal()
    else
        StatusLabel.Text = "Key inválida o expirada"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    end
end)
